ingress:
  enabled: true
  letsencrypt-issuer: "letsencrypt-prod"
  annotations:
    kubernetes.io/ingress.class: nginx
  hosts:
    - host: ton-teleport-develop.rsquad.solutions
      paths:
        - /indexer

service:
  enabled: true