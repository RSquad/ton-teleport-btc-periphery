ingress:
  enabled: true
  letsencrypt-issuer: "letsencrypt-prod"
  hosts:
    - host: ton-teleport-develop.rsquad.solutions
      paths:
        - /indexer

service:
  enabled: true

container:
  port: 3000
