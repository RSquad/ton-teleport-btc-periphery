apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard_test: "1"
configMapGenerator:
- name: dashboards-123
  files:
  - AppLogs.json
  - ContractBalances.json
  - dashboards.json
  - another.json
